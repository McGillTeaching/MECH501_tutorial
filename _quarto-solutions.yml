# Rendered with: quarto render --profile solutions
# Output goes to a separate folder so you never accidentally publish solutions.
project:
  output-dir: _site-solutions

website:
  title: "MECH 501/600 — Computational Turbulence (SOLUTIONS — do not distribute)"
