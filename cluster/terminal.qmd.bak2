---
title: "Terminal"
subtitle: "Bash basics, a first look at vim, and --- later --- SSH"
author: "MECH 501/600 — Computational Turbulence"
date: last-modified
---

::: {.callout-note}
## Two ways to get a terminal
The **default** for this course is the terminal built into **JupyterLab** --- it is
already a shell on the cluster, with nothing to install. Connecting from a terminal
on **your own computer over SSH** is a more advanced option we introduce
[later on this page](#ssh-advanced).
:::

## Open a terminal in JupyterLab

In JupyterLab click the **+** (new Launcher) and choose **Terminal** (or
**File ▸ New ▸ Terminal**). You now have a **bash** shell running on the cluster ---
the same place your notebooks and files live.

## Getting around and looking at files

A handful of commands cover almost everything you need:

```bash
pwd                 # print working directory — where am I?
ls                  # list files here
ls -l               # long listing: sizes, dates, permissions
ls -a               # also show hidden files (names starting with a dot)
cd project          # go into a folder
cd ..               # go up one level
cd ~                # go to your home directory
mkdir runs          # make a new folder
```

Looking *inside* a file without opening an editor:

```bash
cat  log.simpleFoam              # print the whole file
less log.simpleFoam              # page through it (Space scrolls, q quits)
head log.simpleFoam              # first 10 lines
tail log.simpleFoam              # last 10 lines
tail -f log.simpleFoam           # follow a file as it grows (Ctrl-C to stop)
grep "converged" log.simpleFoam  # show only lines containing a word
```

Copying, moving and deleting files:

```bash
cp file.txt backup.txt   # copy
mv old.txt new.txt       # rename / move
rm scratch.txt           # delete — there is no undo
```

::: {.callout-tip}
Press **Tab** to auto-complete file and folder names, and the **↑ / ↓** arrow keys
to scroll back through commands you already typed.
:::

## A tiny bit of vim

`vim` is a text editor that lives in the terminal. The one idea to grasp is that it
has **modes**: you *move around* in **normal** mode and *type text* in **insert**
mode.

```bash
vim system/controlDict     # open (or create) a file
```

| To… | do this |
|---|---|
| start typing | press **`i`** (enter insert mode) |
| stop typing | press **`Esc`** (back to normal mode) |
| save | type `:w` and Enter |
| save and quit | type `:wq` and Enter |
| quit without saving | type `:q!` and Enter |

That is genuinely all you need to edit a case file; everything else in vim is a
bonus.

## Make the shell and vim nicer (optional)

Two hidden files in your home directory configure your shell (`~/.bashrc`) and vim
(`~/.vimrc`). They are optional, but the minimal setup below gives you a readable
two-line prompt, a longer timestamped history, safer `rm`/`cp`/`mv`, and sensible
vim defaults. Create each file once with `vim ~/.bashrc` / `vim ~/.vimrc`, paste,
and save.

::: {.callout-tip collapse="true"}
## `~/.bashrc` --- click to expand
Paste this into `~/.bashrc`, then run `source ~/.bashrc` to apply it in the current
shell.

```bash filename="~/.bashrc"
# ── Prompt ────────────────────────────────────────────────────────────────────
git_branch() {
    git branch 2>/dev/null | grep '^\*' | sed 's/\* //'
}

# Colors
_reset="\[\e[0m\]"
_bold="\[\e[1m\]"
_red="\[\e[38;5;196m\]"
_orange="\[\e[38;5;214m\]"
_yellow="\[\e[38;5;226m\]"
_green="\[\e[38;5;82m\]"
_blue="\[\e[38;5;75m\]"
_gray="\[\e[38;5;245m\]"

PS1="${_gray}┌─${_reset}"                        # top bar
PS1+="${_bold}${_blue}\u${_reset}"               # username
PS1+="${_gray}@${_reset}"                        # @
PS1+="${_bold}${_orange}\h${_reset}"             # hostname
PS1+="${_gray} in ${_reset}"                     # in
PS1+="${_bold}${_yellow}\w${_reset}"             # working directory
PS1+="\$(git_branch | sed 's/.*/ ${_gray}(${_green}&${_gray})${_reset}/')"  # git branch
PS1+="\n${_gray}└─${_reset} "                   # second line prompt

export PS1

# ── History ───────────────────────────────────────────────────────────────────
HISTSIZE=10000
HISTFILESIZE=20000
HISTCONTROL=ignoredups:erasedups       # no duplicate entries
HISTTIMEFORMAT="%Y-%m-%d %H:%M  "     # timestamp every command
shopt -s histappend                    # append instead of overwrite on exit
PROMPT_COMMAND="history -a"            # write to history immediately

# ── Navigation ────────────────────────────────────────────────────────────────
#shopt -s autocd                        # type a directory name to cd into it
shopt -s cdspell                       # fix minor typos in cd paths
shopt -s dirspell                      # fix minor typos in directory completion

# -- Alias
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
alias chmod='chmod -v'
alias ls='ls --color=auto'
alias ll='ls -l --color=auto'
alias la='ls -A --color=auto'
alias ..='cd ..'
alias ...='cd ../..'
alias grep='grep --color=auto'
alias more='/usr/bin/less'

export EDITOR=vim
```
:::

::: {.callout-tip collapse="true"}
## `~/.vimrc` --- click to expand
Paste this into `~/.vimrc`. The persistent-undo option needs its folder to exist,
so run `mkdir -p ~/.vim/undodir` once.

```vim filename="~/.vimrc"
" ── General ───────────────────────────────────────────────────────────────────
set nocompatible
set encoding=utf-8
set history=1000
set undofile                        " persistent undo across sessions
set undodir=~/.vim/undodir

" ── Appearance ────────────────────────────────────────────────────────────────
syntax on
set number                          " line numbers
set relativenumber                  " relative numbers (great for jumping)
set cursorline                      " highlight current line
set scrolloff=8                     " keep 8 lines above/below cursor
set colorcolumn=80,120              " column rulers
set laststatus=2                    " always show status line
set showmatch                       " highlight matching brackets
set termguicolors

" ── Indentation ───────────────────────────────────────────────────────────────
set tabstop=4
set softtabstop=4
set shiftwidth=4
set expandtab                       " spaces not tabs
set smartindent
set autoindent

" C++ / OpenFOAM often uses different style
autocmd FileType cpp setlocal tabstop=4 shiftwidth=4 expandtab
autocmd FileType c   setlocal tabstop=4 shiftwidth=4 expandtab

" ── Search ────────────────────────────────────────────────────────────────────
set incsearch                       " search as you type
set hlsearch                        " highlight matches
set ignorecase
set smartcase                       " case-sensitive if uppercase used
nnoremap <Esc> :nohlsearch<CR>      " clear highlight with Escape

" ── File handling ─────────────────────────────────────────────────────────────
set autoread                        " reload file if changed outside vim
set noswapfile
set nobackup
set hidden                          " switch buffers without saving

" ── Splits ────────────────────────────────────────────────────────────────────
set splitright                      " new vertical split opens on the right
set splitbelow                      " new horizontal split opens below
nnoremap <C-h> <C-w>h
nnoremap <C-j> <C-w>j
nnoremap <C-k> <C-w>k
nnoremap <C-l> <C-w>l

" ── Useful remaps ─────────────────────────────────────────────────────────────
let mapleader = " "                 " space as leader key

nnoremap <leader>w :w<CR>           " save with space-w
nnoremap <leader>q :q<CR>           " quit with space-q
nnoremap <leader>e :Ex<CR>          " open file explorer

" Move lines up/down with Alt+j/k
nnoremap <A-j> :m .+1<CR>==
nnoremap <A-k> :m .-2<CR>==

" Stay in visual mode after indenting
vnoremap < <gv
vnoremap > >gv

" ── Netrw (built-in file browser) ─────────────────────────────────────────────
let g:netrw_banner    = 0           " hide the banner
let g:netrw_liststyle = 3           " tree view
let g:netrw_winsize   = 25
```
:::

## SSH from your own computer {#ssh-advanced}

::: {.callout-note}
## More advanced --- for later
You do **not** need this for the course: the JupyterLab terminal above is enough.
Once command-line access is introduced, you can connect from a terminal on your own
machine (macOS/Linux `Terminal`, or Windows PowerShell / WSL).
:::

```bash
ssh <your-username>@<login-host>   # login host will be posted with this task
```

using the same username and password you created during
[Registration](registration.qmd). Everything above --- navigation, viewing files,
vim, and the `~/.bashrc` / `~/.vimrc` setup --- works exactly the same over SSH.

::: {.content-visible when-profile="solutions"}
::: {.callout-warning}
## Instructor TODO (solutions build only)
Fill in the **SSH login host** for this Magic Castle instance (e.g.
`login1.mech501.calculquebec.cloud` or the login-node IP), and note whether SSH
keys or passwords (and any TOTP) are used.
:::
:::
